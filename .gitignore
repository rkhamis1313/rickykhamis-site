__pycache__/
*.pyc
.env
.env.*
!.env.example
.DS_Store
*.log
.netlify/

# Scratch pages dropped into site/ while checking a layout in a headless
# browser. One of these (_upload.html) reached production before anyone
# noticed, so the underscore prefix is now reserved and ignored.
site/_*.html
